# Crosstalk investigation report

- Session: `20260828T034736Z`
- Evidence integrity audit: **PASS**
- Verification coverage: 0.0%
- Scientific release: **NOT ESTABLISHED**
- Claims: 4
- Evidence artifacts: 10
- Verification records: 0

## Problem

Derive or sharply falsify an exact dyadic spacetime-tile composition algebra for the Rule 30 center cell. The measured anomaly is exact nonrandom Morton/quadtree reuse in both axes. A valid candidate must define its state, an exact parent-from-children or doubling operation, a poly(log n) state and work bound, center-bit extraction, and a deterministic held-out falsification probe. Treat compression, images, rotations, mirrors, zero patterns, and 3D embeddings only as evidence lenses. Do not claim a solution without the composition identity and bound.

## Claims

- **Inference / Open** `turn-1-claim-51` — The sub-area but super-polylogarithmic tile vocabulary in ARM7 is consistent with Rule 30 having **moderate** redundancy (as any deterministic system must) without that redundancy being capturable by a fixed finite algebra.
- **Inference / Open** `turn-1-claim-53` — The non-2-automaticity (ARM6) is a **hard negative**: it eliminates the entire class of finite-state transducers reading binary digits of n. This is the strongest structural result obtained.
- **Proposal / Open** `turn-1-claim-67` — The discriminating test is already implicit in ARM7: measure tile vocabulary V(n) for horizons n = 2^k, k = 6..16. If V(n)/polylog(n) → constant, composition may exist. If V(n) ~ n^α for α > 0, composition is implausible.
- **Conjecture / Open** `turn-1-claim-79` — No exact dyadic composition algebra with poly(log n) state exists for the Rule 30 center cell. The measured anomaly (sub-area tile reuse) reflects deterministic redundancy intrinsic to any ECA, not an exploitable algebraic structure.

## Verification records

_No objective verification records were recorded._

## Audit findings

No evidence-chain findings.
