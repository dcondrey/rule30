# Symbolic roundtable: lateral-thinking sparks on Rule 30

Status: **UNEXECUTED, DELIBERATELY UNFILTERED SPARKS, NOT RESULTS.**
This run asked four models to juxtapose Rule 30 against unrelated
domains (other sciences, magic, science fiction, etc.) and follow an
analogy before it can be justified.  Absurd and wrong were explicitly
invited; only "uninteresting" was penalized.  So a low idea-count or a
half-formed spark is not this run failing — it is the run doing its
job, which is coverage of unfamiliar territory, not correctness.
Nothing below is PROVED or MEASURED.  Most entries are not even
formal claims yet; they are analogies that would need real work to
become a checkable proposal at all, and THEN the same treatment as any
other route in `PATH.md`: a precise restatement, a Rule 90 check run
in actual code, a kill condition, and an owner.  Treat this document
as raw material for triage, not a triage list itself.

Calls: 20 (0 errored). Roster (final model per slot,
after any live swaps):
- `openai/gpt-5.6-terra-pro` (GPT-5.6): deep System-2 formal logic and proof trees
- `deepseek/deepseek-v4-pro` (DeepSeek-V4): RL reasoner for circuit-complexity verification
- `x-ai/grok-4.6` (Grok-4.6): lateral mathematical reasoning, unconstrained algebraic exploration
- `meta-llama/llama-4-maverick` (Llama-4-Maverick): open-weights anchor for adversarial verification

**Live model swaps during the run:**
- DeepSeek-V4: `deepseek/deepseek-v4-pro-0813` -> `deepseek/deepseek-v4-pro` at call 2 (response yielded no extractable hypothesis/refutation/obligation)

## Candidate directions surfaced (unverified, unexecuted)

- **GPT-5.6-H1.0**: stage magic, especially “forcing” and the magician’s outs.}  
Rule 30’s left-permutivity makes every local update feel like a forced card: once the centre and right inputs are known, the left input is not chosen but dictated. Turn the usual perspective around. Do not ask how much of the past determines the centre; assume a periodic centre performance has been announced in advance, and ask what increasingly elaborate “outs” the surrounding spacetime must prepare in order to sustain that performance.

A period-\(p\) centre trace gives equations
\[
x_{0,t+p}=x_{0,t}.
\]
For each \(t\), the Rule-30 relation lets one solve for a predecessor-side bit:
\[
x_{-1,t}=x_{0,t+1}\oplus (x_{0,t}\lor x_{1,t}).
\]
Thus the alleged periodic centre does not merely constrain a fixed-width strip: it creates a forced transcript on a zig-zagging “magician’s sleeve” that can be chosen adaptively to follow whichever right-hand bits are inconvenient. The object to seek is a \emph{forcing debt}: after one temporal period, compare the forced left-boundary transcript with its shifted copy. If their disagreement cannot be stored locally but must be exported farther right (or farther left) each cycle, then periodicity demands an infinite stock of hidden outs. The lone seed has only a finite initial stock.

The crucial distinction from a fixed-depth ladder is that the boundary is not held at a fixed spatial location. It is an adaptive cut through spacetime, selected from the purported period word itself—like following the hand that is doing the trick rather than inspecting a fixed sleeve pocket. A useful formal target would be a cut \(\gamma\) carrying a finite “debt token” whose update under one period is monotone, and whose value is zero outside the seed cone. Periodicity would force the token to recur; the forcing calculation would show it strictly drifts. This is closer to a no-perfect-con proof than a propagation-depth argument.

\emph{Rule-90 gut check:} likely promisingly Rule-30-specific, since for Rule 90 the analogous forced bit is linear and period-\(p\) cancellation can make the forcing debt vanish identically; its eventual-zero centre trace supplies exactly the successful “out.”
- **GPT-5.6-H1.1**: molecular chaperones and kinetic proofreading in protein folding.}  
A protein can have many locally plausible partial folds, yet a chaperone repeatedly tests and rejects folds that have the wrong timing; the energy cost of rejection prevents a bad structure from surviving indefinitely. Treat a hypothetical periodic centre word as a repeatedly presented substrate. Instead of seeking a static incompatible neighbourhood, seek a \emph{timing-sensitive rejection event}: a finite spacetime motif that may be locally extendable once, but when presented every \(p\) ticks necessarily creates a one-way defect which Rule 30 cannot erase.

The Rule-30 object would be a “chaperone motif” \(M\): perhaps two nearby diagonal histories together with their relative temporal phase. Its state is not simply a finite strip word; it includes an age or phase counter recording when a local \(1\)-run was last cut, merged, or exposed to a right neighbour. Under the alleged centre period, \(M\) is fed the same central substrate each cycle. One wants a lemma of the flavour:

*every \(p\)-periodic feed either produces a rejection scar at least once per \(K(p)\) cycles, or belongs to a special defect-free family; the lone-seed history cannot enter that family because its initial expansion necessarily nucleates a scar.*

A scar should be something less geometric than a particle count: an irreversible ambiguity-resolution event. Rule 30 has the asymmetric gate \(C\lor R\), so a right-side \(1\) can mask the centre while a right-side \(0\) exposes it; the proposal is that repeated masking/unmasking leaves a phase record in left-forced predecessors. In biochemical language, the system spends “information free energy” each time it corrects a misleading local continuation. A periodic centre would require indefinitely recycling that expenditure without a reset mechanism. The formal payoff could be P1, or—if scars occur with a controlled frequency and have parity-balanced emissions—an unusual route toward P2.

The first experimental task is not a giant trace scan: enumerate small periodic feeds as \emph{in vitro substrates}, search for local motifs whose return map has transient scars versus genuine cyclic regeneration, then inspect whether the actual lone-seed cone supplies the required nucleation condition.

\emph{Rule-90 gut check:} this should fail for Rule 90 in an informative way: linear superposition permits scars to cancel or pass through without biochemical-style proofreading, and its eventual-zero centre is the defect-free folded state.  

\textbf{Open questions}
- Can an adaptive forcing cut be defined so that its update is exact, rather than covertly becoming another fixed-width boundary construction?
- For small candidate periods \(p\), does forced-transcript comparison exhibit a directional drift, a conserved quantity, or merely bounded phase noise?
- Are there finite Rule-30 motifs with an empirically irreversible “scar” under repeated centre-word feeding, and can irreversibility be certified without enumerating an expanding strip?
- Does the lone-seed cone necessarily nucleate one of those motifs, or can it enter a scar-free regenerative class?
- **DeepSeek-V4-H2.0**: **Spark 1 — borrowed from: the economics of *seigniorage* and Gresham’s law in a bimetallic currency system.**  
A government that mints coins of two metals (say gold and silver) at a fixed legal ratio finds that the overvalued metal drives the undervalued one out of circulation — bad money chases out good. The “bad” coin is the one whose face value exceeds its commodity value, and it circulates while the “good” coin is hoarded or exported. Now treat Rule 30’s centre column as the legal mint, and the two “metals” as the two possible local continuations of a finite centre prefix: the continuation that is *easier* to extend consistently (the overvalued one) and the continuation that is *harder* but more faithful to the lone-seed constraint (the undervalued one). The hypothesis is that any candidate periodic centre word acts as a fixed bimetallic ratio, and the lone-seed initial condition is a shock that floods the system with the “good” metal — forcing the “bad” metal to circulate in ever-larger denominations (longer runs, wider spatial excursions) until the system breaks the fixed ratio or exhausts its finite stock of good metal.
- **DeepSeek-V4-H2.1**: The Rule-30 object would be a *mint tableau*: for a given centre prefix \(w\) of length \(L\), classify its legal continuations by the minimal width of the left-past cone required to realise them without contradiction. The “good” metal is the continuation that demands a wider cone; the “bad” metal is the one that can be realised cheaply. A periodic centre word of period \(p\) would enforce a fixed exchange rate: every \(p\) steps, the same prefix reappears and demands the same mixture of good and bad continuations. But the lone seed initially supplies only the good metal (the unique narrow cone), and Rule 30’s left-permutivity acts as the arbitrage mechanism that converts good metal into bad metal at a rate fixed by the local rule table. The question becomes whether this conversion can be a closed cycle, or whether it necessarily leaks good metal outward — a *balance-of-payments* crisis in spacetime. One would look for a conserved quantity (a “bullion reserve”) defined on finite spacetime patches that is strictly positive in the seed cone, invariant under the local rule *except* at the centre-column mint, and strictly decreasing at each centre-column event that chooses the bad continuation. Periodicity would force the reserve to hit zero in finite time, contradicting the infinite past of a bi-infinite periodic centre (or forcing the period to be a transient that cannot reach the seed).
- **DeepSeek-V4-H2.2**: *Rule-90 gut check:* Rule 90’s additive linearity makes the two “metals” interchangeable — every continuation has the same cone-width cost because the rule is left-permutive in a linear way, so the bimetallic distinction collapses and the reserve is identically zero. The eventual-zero centre is the unique coin that costs nothing to mint.
- **DeepSeek-V4-H2.3**: **Spark 2 — borrowed from: the *common knowledge* and *agreeing to disagree* literature in epistemic game theory (Aumann 1976).**  
Two Bayesian agents with common priors cannot “agree to disagree” about a posterior probability if their information partitions are common knowledge. The theorem’s engine is that iterated knowledge operators force a convergence of beliefs once the event of disagreement itself becomes public. Now treat the centre column and the right-half-plane as two agents who observe overlapping but distinct subsets of the spacetime configuration. The centre column “knows” its own history; the right-half-plane “knows” the right-past cone. They share a common prior: the Rule 30 local update rule. A periodic centre column would be a state of perpetual disagreement — the centre asserts a periodic sequence, while the right-half-plane, if it could speak, would assert a non-periodic forcing constraint inherited from the lone seed. The Aumann machinery suggests that if this disagreement is *common knowledge* between the two agents (i.e., if the fact of periodicity is itself encoded in the configuration in a way both can eventually deduce), then the disagreement must be impossible — the centre cannot be periodic unless the right-half-plane’s information already forced it to be so, which the lone seed precludes.
- **Grok-4.6-H3.0**: the Pythagorean comma and the wolf fifth in just intonation.}
Stacking pure 3:2 fifths never closes the octave; a remainder accumulates, and a keyboard either harbours a wolf interval or spreads the error by tempering. Treat a candidate period-\(p\) centre word as an attempt to close that circle: each local Rule-30 update along a loop that rides the centre for \(p\) ticks and returns along a finite spatial chord is one “fifth.” Rule 30’s \(\lor\) is the pure (untampered) interval. The holonomy around the loop is then a comma, and a consistent periodic filling would require the comma to vanish.

The Rule-30 object is a transport \(H(w,k)\) for a word \(w\) of length \(p\) and chord width \(k\): carry a left-boundary state around the loop, using the unique left-permutive preimage on the centre-hugging leg and the forward table on the chord. Periodicity plus a filling of the interior asks \(H=0\). The wolf is that \(H\) records the discrepancy between \(C\lor R\) and linear (XOR) transport; it is identically zero only if \(R\equiv 0\) along the chord — which is precisely Rule 90’s successful out, the eventually-zero column, and is unavailable once the lone seed has launched a right-going 1. Unlike a fixed-depth strip, the chord is a closing device, not a boundary one keeps forever; the quantity of interest is the failure of the loop to be a homomorphism, not how far a bit propagates.

\emph{Rule-90 gut check:} transport is linear over \(\mathbb{F}_2\), stacking closes, the comma is the zero vector, matching the eventually periodic centre. If some nonlinear \(w\) also kills \(H\) for all large \(k\), that would be a tempered period and the analogy would have named a family rather than an obstruction.
- **Grok-4.6-H3.1**: garden-path sentences and incremental parsing (Bever’s “the horse raced past the barn fell”).}
A hearer commits to a local structure that later material forces them to rebuild; cost lives in a stack that is not always emptied. Read the centre column top-to-bottom as the sentence, the right-hand cells as already-consumed context, and the left-permutive preimages as predicted categories. A periodic centre is the same string offered again and again, which parses cleanly only if the stack is cyclic. Rule 30 is a garden-path grammar: \(C\lor R\) is the reduced-relative ambiguity, a right-hand 1 “reduces” the centre, and a later 0 pops the prediction incorrectly and leaves residue. The lone seed is the empty stack plus one filled cell — a path that never receives a clean reanalysis.

The object is a residue \(\rho_t\) in a small monoid, updated from the triple at columns \(-1,0,1\) but with the left bit supplied by permutivity from the \emph{next} centre bit (the parser’s prediction, not a stored strip). For P1: \(\rho\) refuses to be periodic when the centre is, unless the right column supplies a thematic role the seed cone never does. For P3 this is not circuit complexity of a function family; it is the work of one parser on one string, and the non-invertible reduce (the \(\lor\)) can force \(\Omega(n)\) reanalysis operations by time \(n\). Finite data still cannot finish an infinitary claim, but a certified one-way reduce would at least be a different currency than a kernel rank.

\emph{Rule-90 gut check:} the grammar is unambiguous and left-linear; the parser is a finite automaton whose stack empties after the transient, so the garden path resolves and stays resolved.
- **Llama-4-Maverick-H4.0**: the concept of *anamorphosis* in art and optics.

An anamorphic image appears distorted when viewed from most angles but resolves into a coherent picture from a specific vantage point. Treat the centre column of Rule 30 as the anamorphic image and the surrounding spacetime configuration as the optical system that either supports or distorts it. A periodic centre column would be a "correct" viewing angle that somehow aligns with the distorted spacetime around it. The hypothesis is that the lone seed's initial condition creates an irreconcilable distortion that prevents the centre column from being periodic.

The Rule-30 object would be a *distortion tensor*: a measure of how the spacetime configuration around the centre column deviates from a hypothetical periodic "viewing angle." One would need to define a tensor field on the spacetime lattice that captures the discrepancy between the actual Rule-30 update rule and a linearized (Rule-90-like) transport along a loop that hugs the centre column. The distortion tensor should be strictly positive in the seed cone and ideally have a monotonicity property under the Rule-30 dynamics. Periodicity of the centre would force the tensor to vanish or become periodic; the lone seed's distortion would prevent this.

*Rule-90 gut check:* Rule 90's linearity makes the distortion tensor vanish identically, as its update rule is already a homomorphism, matching the eventually periodic centre.
- **Llama-4-Maverick-H4.1**: the *garden of forking paths* trope in science fiction and literary theory (Borges' "The Garden of Forking Paths").

In this trope, every possible outcome of an event creates a new branch of reality, leading to a labyrinthine structure of diverging paths. Treat the centre column of Rule 30 as a narrative thread that forks into multiple possible continuations at each step. The surrounding spacetime configuration represents the ever-branching garden of possibilities. A periodic centre column would imply a narrative that somehow rejoins its past, creating a closed timelike curve in the space of possible stories.

The Rule-30 object would be a *fork-count*: a measure of the number of distinct spacetime paths that are consistent with a given centre prefix. One would need to show that under the alleged centre period, the fork-count either grows without bound or is reset in a way that the lone seed cannot sustain. The fork-count should be related to the number of legal left-past cones that can be constructed for a given centre word.

*Rule-90 gut check:* Rule 90's additive structure allows for a clean reconciliation of forking paths, as every possible continuation can be superimposed linearly; its eventually zero centre is a narrative that terminates rather than loops back.

### Screen-out

The Aumann / agreeing-to-disagree spark from DeepSeek-V4-H2.1 is refuted because common knowledge in epistemic game theory requires a public event or information partition, which either becomes column-blind or migrates like a free boundary. Iterated knowledge operators in this context quantify over information partitions, falling afoul of the measure-zero single-orbit gap (T3).

### Open questions

- Does the distortion tensor stabilize, drift, or oscillate as one moves away from the centre column? Stabilization would indicate a periodic "viewing angle," while drift would suggest an irreconcilable distortion.
- Can the fork-count be related to a known property of Rule 30, such as the complexity of its left-past cones or the frequency of certain spacetime motifs?
- Are there other "vantage points" (spatial chords or temporal loops) from which the centre column appears periodic, and can these be classified or ruled out?
- Does the fork-count or distortion tensor bound work or merely depth, avoiding the VDF trap?
- **GPT-5.6-H5.0**: **Spark 1 — borrowed from: \(2\)-adic carries, Mahler functions, and the arithmetic of eventually periodic decimals.**  
An eventually periodic binary word is a rational number in disguise: its generating series \(A(u)=\sum_{t\ge0}a_tu^t\) is rational over \(\mathbb F_2(u)\), just as a repeating base-10 expansion is rational. The alien question is therefore not “can the centre recurrence be recognized by a machine?” but: *what sort of arithmetic object is the centre generating series forced to be by the nonlinear carry term in Rule 30?* Rule 30 is
\[
x_{i,t+1}=x_{i-1,t}\oplus x_{i,t}\oplus x_{i+1,t}\oplus x_{i,t}x_{i+1,t}.
\]
The final product is a carry-like event: two adjacent occupied digits create a contribution not seen by linear addition.
- **GPT-5.6-H5.1**: Package a whole row as a Laurent polynomial \(P_t(z)=\sum_i x_{i,t}z^i\) over \(\mathbb F_2\). The first three terms evolve linearly by multiplication with \(z^{-1}+1+z\); the \(x_{i,t}x_{i+1,t}\) term is a shifted coefficientwise product, a “digitwise carry operator.” Seek a bivariate series
\[
F(z,u)=\sum_{t\ge0}P_t(z)u^t,
\qquad A(u)=[z^0]F(z,u).
\]
The hoped-for theorem is not that \(F\) is rational—it almost certainly is not—but a contrapositive: if the diagonal/constant-term series \(A(u)\) were rational, then repeated carry propagation would have to become *arithmetically confined*: after some scale, the support of certain coefficientwise products would obey a finite family of exact cancellation identities. The seed’s expanding one-sided edge may provide a place to show that these carries keep creating new \(2\)-adic valuation classes.
- **GPT-5.6-H5.2**: This is a rationality obstruction rather than a finite-data kernel bound. One could search for a scale relation involving \(A(u),A(u^2),A(u^4)\), not because Rule 30 is automatic, but because Frobenius squaring in characteristic two exposes which terms are genuine carries:
\[
(P_t(z))^2=P_t(z^2).
\]
A periodic centre would impose rational compatibility across all dyadic scales; Rule 30’s adjacent-product term may violate that compatibility at an identifiable sequence of scales.
- **GPT-5.6-H5.3**: *Rule-90 gut check:* Rule 90 has no carry term, its row series evolves linearly, and the lone-seed centre series is a polynomial; this route is specifically aimed at the nonlinear \(x_{i,t}x_{i+1,t}\) residue.
- **DeepSeek-V4-H6.0**: **Spark 1 — borrowed from: the *paradox of the unexpected hanging* and self-referential epistemic collapse in logic puzzles.**  
A judge tells a prisoner he will be hanged on a weekday next week, but the hanging will be a surprise: the prisoner will not be able to deduce the day in advance. The prisoner reasons backwards from Friday (if he survives Thursday, Friday cannot be a surprise, so it cannot be Friday; then Thursday becomes the last possible day, so it cannot be a surprise either, and so on) and concludes he cannot be hanged at all — whereupon the hangman arrives on Wednesday, genuinely surprising him. The paradox turns on the fact that a *publicly announced constraint on deducibility* interacts with a *backwards induction* to produce a conclusion that is both logically compelled and empirically false.
- **DeepSeek-V4-H6.1**: Now treat the centre column of Rule 30 as the judge's announcement: "I am periodic with period \(p\)." The surrounding spacetime is the prisoner, attempting to deduce *where* the period must break by backwards induction from the infinite future. The backwards induction is not a fixed-width ladder; it is an *epistemic regress* that propagates a "surprise token" — a bit or small label that says "at this spacetime location, the announced periodicity forces a contradiction unless some earlier location already broke it." The token propagates backwards in time along the unique left-permutive preimage path, but its *value* depends on whether the right-hand neighbour at that location is a 0 or a 1 — and that right-hand neighbour is itself constrained by the periodicity announcement further in the future. The regress is: to know whether the token at \((x,t)\) is a genuine contradiction, I must first know whether the token at \((x+1, t+1)\) is a contradiction, which depends on \((x+2, t+2)\), and so on. The lone seed is the hangman's calendar: a finite initial condition that supplies a "surprise" (a 1 where the backwards induction predicted a 0) at a specific finite time, breaking the regress and forcing the prisoner to admit that the announcement was false.
- **DeepSeek-V4-H6.2**: The Rule-30 object is a *surprise token* \(\sigma(x,t) \in \{0,1,\bot\}\) defined by backwards induction from the alleged periodicity. For a candidate period-\(p\) centre word \(w_0 \ldots w_{p-1}\), define \(\sigma(x,t)\) for \(t\) large and \(x\) near the centre as follows. If the centre bit at time \(t\) is \(w_{t \bmod p}\), then the left-permutive preimage equation
\[
x_{-1,t} = x_{0,t+1} \oplus (x_{0,t} \lor x_{1,t})
\]
determines \(x_{-1,t}\) uniquely. But \(x_{1,t}\) is not given; it must be consistent with the periodicity announcement at later times. So \(\sigma(x,t)\) asks: is there an assignment to all right-hand cells \((x' \ge x, t' \ge t)\) that satisfies the Rule-30 update, matches the announced centre word, and is compatible with the lone-seed initial condition at \(t=0\)? If yes, \(\sigma = 0\); if no, \(\sigma = 1\); if the question depends on unresolved future tokens, \(\sigma = \bot\). The backwards induction is the propagation rule for \(\bot\): a token at \((x,t)\) is \(\bot\) until all tokens at \((x+1, t+1)\) that it depends on are resolved. The paradox is that for the lone seed, the backwards induction from infinity never resolves — the token remains \(\bot\) for all sufficiently large \(t\) — but at the finite time when the seed's expanding cone reaches the centre, a genuine 1 appears that was not forced by the periodicity announcement, and the token flips to 1, contradicting the announcement. The formal target is a proof that the set of \((x,t)\) where \(\sigma = \bot\) is infinite for any \(p > 0\), and that the seed's cone intersects this set at a point where the Rule-30 update forces \(\sigma = 1\).
- **DeepSeek-V4-H6.3**: *Rule-90 gut check:* Rule 90's linear preimage equation \(x_{-1,t} = x_{0,t+1} \oplus x_{0,t} \oplus x_{1,t}\) makes the backwards induction collapse: the token at \((x,t)\) depends on \((x+1,t+1)\) only through a linear term, and the regress terminates because the all-zero future is a consistent completion for any finite prefix. The surprise token resolves to 0 everywhere, matching the eventually-zero centre.
- **Grok-4.6-H7.0**: **Spark 1 — borrowed from: Feynman’s ratchet-and-pawl (the Brownian ratchet as a failed perpetual-motion machine).**
A paddle in a thermal bath plus a spring-loaded pawl looks as if it should rectify noise into one-way rotation; it does not, because the pawl is itself thermal. A cyclic click-train survives only while two baths sit at different temperatures, and that difference is spent. Treat Rule 30’s \(\lor\) as the pawl and a candidate periodic centre word as the claim that the wheel clicks forever with period \(p\). The lone seed is not “a special configuration” so much as a finite entropy deficit: one cold cell in an otherwise infinite heat bath. The nonlinear term \(x_{i,t}x_{i+1,t}\) is the pawl engaging; XOR is the unbiased paddle.
- **Grok-4.6-H7.1**: The Rule-30 object is a two-bath ledger, not a particle count and not a strip invariant. On each row let \(Q_L(t)\) be the empirical entropy of the left-preimage bits that left-permutivity writes from the centre, and \(Q_R(t)\) the entropy of a co-moving right neighbourhood whose width may grow (the pawl’s own jiggling). The seed starts with \(Q_L-Q_R\) strictly positive. Each engagement of \(\lor\) is an attempted rectification that, Feynman-style, can do net work only while the difference is positive, and that drains it. Periodicity of the centre would force the ledger into a cycle, hence a recurrent temperature difference, i.e. a perpetual-motion engine built from a one-way gate. The hoped-for lemma is that Rule 30 admits no such cyclic engine: once the seed’s deficit is diluted over the light cone the pawl thermalizes and the click-train cannot be periodic. This is a second-law obstruction to a cyclic machine, not a forcing-depth argument and not a conserved bullion stock.
- **Grok-4.6-H7.2**: *Rule-90 gut check:* there is no pawl, only reversible XOR; both baths are the same linear process, the ledger is identically zero, and the wheel simply stops — the eventually-zero centre.
- **Grok-4.6-H7.3**: **Spark 2 — borrowed from: the army-ant mill (circular milling) as a stigmergic death spiral.**
Army ants follow the pheromone of the ant in front. If the column ever closes, they mill until they die. Mills are a high-density failure mode of trail-following; they do not nucleate from a single ant on an open raid. Read a periodic centre as a mill in time: each centre bit is the ant that follows the bit from \(p\) ticks earlier, with \(C\lor R\) as the local “follow that trail” rule. The lone seed is one ant. Stigmergy from one agent produces a raid column (the light cone), not a loop.
- **Llama-4-Maverick-H8.0**: **Spark 1 — borrowed from: the *cut elimination* and *proof normalization* in sequent calculus and natural deduction.**  
A proof theorist seeks to eliminate unnecessary detours (cuts) in a formal proof, simplifying it to a normal form. Treat the centre column of Rule 30 as a proof being constructed step by step, with each local configuration representing a sequent. The Rule 30 update rule is then a set of inference rules that construct the next line of the proof. A periodic centre column would imply a proof that repeats itself, potentially indicating a cycle in the proof construction process. The lone seed initial condition is akin to an axiom — a starting point that launches the proof. The hypothesis is that Rule 30's dynamics prevent the construction of a periodic proof due to the accumulation of "cuts" or irregularities that cannot be normalized.
- **Llama-4-Maverick-H8.1**: The Rule-30 object would be a *normalization measure*: a quantification of how "normalizable" the proof represented by the centre column is at each step. One would need to define a measure on the space of proofs (centre words) that captures the degree of normalization achievable under the Rule 30 dynamics. Periodicity of the centre would imply a proof that is somehow "recursively normalizable," which may be at odds with the lone seed's initial irregularity.
- **Llama-4-Maverick-H8.2**: *Rule-90 gut check:* Rule 90's linearity makes it akin to a proof system where every step is deterministic and reversible, leading to a straightforward normalization process. The eventually zero centre is a "proof" that terminates, having reached a trivial conclusion.
- **Llama-4-Maverick-H8.3**: **Spark 2 — borrowed from: *strange attractors* and the *butterfly effect* in chaotic dynamical systems.**  
In chaotic systems, a strange attractor is a complex set that attracts trajectories, and the butterfly effect describes how small changes in initial conditions lead to drastically different outcomes. Treat the centre column of Rule 30 as a trajectory in a high-dimensional space, with the Rule 30 update rule defining the dynamics. A periodic centre column would be a stable orbit, while the lone seed is a perturbation that tests the stability of this orbit. The hypothesis is that Rule 30's dynamics are chaotic enough that the lone seed's "butterfly effect" prevents the centre column from settling into a periodic orbit.
- **GPT-5.6-H9.0**: **Spark 1 — borrowed from: synchronization theory for coupled chaotic systems, plus epidemiological “damage spreading.”**  
Do not ask whether the centre word can be generated by a finite device; ask whether two copies of the *same actual orbit*, offset by \(p\) time steps, can remain perfectly synchronized on one spatial sensor forever. A purported eventual period \(p\) gives exactly such an experiment: put the Rule-30 diagram beside its time translate and observe the vertical seam at \(x=0\). The two worlds are different finite perturbations near their initial rows, yet their centre sensors report equality forever after the transient.
- **GPT-5.6-H9.1**: Let \(X_i(t)\) be the lone-seed diagram and compare
\[
Y_i(t)=X_i(T+t+p),\qquad Z_i(t)=X_i(T+t).
\]
Both obey Rule 30, begin from finite-support rows, and eventual \(p\)-periodicity says \(Y_0(t)=Z_0(t)\) for every \(t\ge0\). Their pair \((Y,Z)\) is a four-state cellular automaton; call \(D_i(t)=Y_i(t)\oplus Z_i
- **DeepSeek-V4-H10.0**: **Spark 1 — borrowed from: the *two-envelope paradox* and the logic of *swap arguments* in decision theory.**  
You are handed two envelopes, one containing twice the money of the other. You pick one, open it, and are offered the chance to swap. A naive expectation calculation says you should always swap, regardless of what you see — a symmetry argument that cannot be right because it applies identically to both envelopes. The paradox is resolved by noticing that the "always swap" argument implicitly assumes a uniform prior over all possible dollar amounts, which is not a probability distribution. The symmetry is broken by the fact that the money had to come from somewhere finite.
- **DeepSeek-V4-H10.1**: Now treat a candidate period-\(p\) centre word as the "always swap" symmetry. The two envelopes are the centre column and its own \(p\)-step time-translate. The claim "they are equal forever" is the symmetry assertion. The Rule 30 update rule is the envelope-opener: at each time step, the left-permutive preimage equation forces a specific relation between the centre bit, its right neighbour, and the next centre bit. If the centre is periodic, this relation must hold with the same right-neighbour pattern repeating — a symmetry that says "whatever right-neighbour pattern supports this centre word, its \(p\)-step translate must also support it." The paradox is that this symmetry, applied inductively into the right half-plane, forces the right-neighbour pattern to be drawn from a "uniform prior" — an infinite, unstructured background — but the lone seed is a finite, structured initial condition that breaks the symmetry. The formal object is a *swap operator* \(S_p\) on right-half-plane configurations: given a right-half-plane filling \(R\) that supports the centre word \(w\), \(S_p(R)\) is the \(p\)-step time-translate of \(R\), shifted to align with the centre. Periodicity asserts \(S_p(R) = R\) for the actual filling generated by the seed. The paradox lemma would be that \(S_p\) is an isometry with no finite-support fixed points except the all-zero configuration — and the seed's right-half-plane is not all-zero.
- **DeepSeek-V4-H10.2**: *Rule-90 gut check:* Rule 90's linearity makes \(S_p\) the identity on the subspace of solutions to the linear recurrence; the all-zero fixed point is the unique finite-support solution, matching the eventually-zero centre. The paradox collapses because the "uniform prior" is actually a well-defined algebraic object (the additive group), and the seed's finite support selects the zero solution uniquely.
- **DeepSeek-V4-H10.3**: **Spark 2 — borrowed from: *prion diseases* and *template-directed misfolding* in molecular biology.**  
A prion is a misfolded protein that does not just malfunction — it catalytically converts properly folded copies of the same protein into the misfolded shape. The disease propagates because the misfolded form is a template that creates more of itself, but it can only do so by consuming the properly folded substrate. The key dynamic is that the prion form is *autocatalytic but parasitic*: it cannot arise de novo from an empty system; it needs at least one misfolded seed, and it spreads only while properly folded substrate remains available. When substrate runs out, propagation stops — the prion does not create new protein, only corrupts existing stock.
- **Grok-4.6-H11.0**: Wolpert’s French-flag model and Cooke–Zeeman clock-and-wavefront somitogenesis.}
A morphogen secreted from one pole paints a monotonic gradient; stripes (pair-rule, somites) appear only when that gradient is read by a *separate* cellular oscillator, and the oscillator’s phase is frozen as a determination front sweeps past. A gradient alone never oscillates at a fixed station. Read the lone seed as the unique morphogen source, the right-going 1 as the determination front, and a putative period-\(p\) centre as a claim that somites are forming at \(x=0\) forever. Rule 30 supplies no per-cell clock state beyond the bit itself: the only candidate “oscillator” *is* the centre bit, while the only candidate front recedes from that bit at speed 1. Phase-lock between a clock and a wavefront whose separation is \(\Theta(t)\) would require an independent delay line; \(C\lor R\) is a local threshold, not a delay line. The Rule-30 object is a *front–clock split* \(\Delta(t)=t_{\text{front}}-t_{\text{phase}}\): under alleged centre period, \(\Delta\) must remain in a finite set of residues, but the seed cone forces \(\Delta(t)\sim t\). Periodicity is then the developmentally illegal move of asking a French flag to do the work of a segmentation clock.

\emph{Rule-90 gut check:} linear superposition *is* a standing-wave clock; source and nodes are the same binomial identity, and the centre collapses to the empty node after one tick — the “oscillator” is not asked to run at a station the front has left.
- **Grok-4.6-H11.1**: the bootstrap / predestination paradox (Heinlein’s “By His Bootstraps,” the watch that is never manufactured).}
An object that exists only because it will be carried back to become itself has no manufacturing event; introducing a manufacturing event into the same world-line is a contradiction, not a delay. A periodic centre is a bootstrap on a bit string: each symbol is caused by the symbol \(p\) ticks earlier, a closed manufacturing loop with no first writing. The lone seed *is* a first writing — an exogenous object inserted at \(t=0\). Those two stories cannot share a channel. Because of \(\lor\), origin is not a linear superposition: the local map is coalescent, so one can define a well-founded *origin trace* \(o(t)\) that follows each centre bit backward through the unique left-permutive parent until it either hits another centre bit or hits the seed cell. Eventual period \(p\) would make \(o\) a permutation of a cycle whose basin does not contain the seed; the actual diagram has the seed in every sufficiently long trace. This is not backwards epistemic induction (no surprise token, no knowledge operators) and not a swap-symmetry on envelopes; it is a claim about whether a bit is allowed to have a birthday.

\emph{Rule-90 gut check:} origins superpose over \(\mathbb{F}_2\); the eventual zeros are manufactured by cancellation, which is a legitimate bootstrap of nothing, and the seed’s contribution to column 0 is exactly the finite polynomial that dies.

\subsection*{Screen-out}
The two-envelope / swap-operator spark is the measure-zero single-orbit gap in costume. “The always-swap argument assumes a uniform prior that is not a distribution, and finiteness of the money breaks the symmetry” is T3/E restated: almost every right-half-plane filling could fix \(S_p\), the lone seed is one finite-support point, and declaring \(S_p\) an isometry with no finite-support fixed points besides zero either (i) uses linearity (Rule 90) or (ii) quantifies over fillings. It does not pin this orbit.

\subsection*{Open questions}
- Can \(\Delta(t)\) be read from a centre window plus a *moving* front marker without becoming a fixed-depth strip (free boundary) or a density-zero-continuous functional (column-blind)?
- For the origin trace: is coalescence under \(\lor\) strong enough that \(o(t)\) is a *function* rather than a relation, and does a single certified coalescence at some \(t_0\) already poison every later \(p\)-cycle, or can traces merge and re-split?
- Does origin-tracing cost work or only depth — i.e., can one skip a block of \(p\) steps once a cycle is hypothesized, or does each \(\lor\) destroy the information needed to jump?
- Does any nonlinear elementary rule with a lone seed admit a bootstrap centre, or is coalescence plus one-sided light cone already the obstruction?
- **Llama-4-Maverick-H12.0**: **Spark 1 — borrowed from: the *computational complexity* of *matchgate circuits* and their relation to *free fermions*.**  
Matchgate circuits are a restricted class of quantum circuits that can be efficiently simulated classically due to their connection to free fermionic systems. The computation is based on nearest-neighbor interactions that preserve certain symmetries. Treat the Rule 30 update as a *fermionic* process where the presence or absence of a "particle" (1 or 0) at a site is governed by a nearest-neighbor interaction that includes a nonlinear term akin to an interaction vertex. A periodic centre column would imply a *sustainable fermionic state* that is maintained by the Rule 30 dynamics. The lone seed is an initial "injection" of a particle that tests whether the system can sustain a periodic fermionic state.
- **Llama-4-Maverick-H12.1**: The Rule-30 object would be a *fermionic correlation measure*: a quantification of how the presence of a particle at the centre column correlates with the state of its neighbors over time. One would need to show that under the alleged centre period, these correlations either grow without bound or are reset in a way that the lone seed cannot sustain. The correlation measure should be related to the complexity of simulating the Rule 30 dynamics using matchgate circuits.
- **Llama-4-Maverick-H12.2**: *Rule-90 gut check:* Rule 90 is linear and can be seen as a free fermionic system where particles evolve independently; its eventually zero centre is a state that decays rather than sustains a complex fermionic correlation.
- **Llama-4-Maverick-H12.3**: **Spark 2 — borrowed from: *homotopy continuation* and *numerical algebraic geometry*.**  
Homotopy continuation is a numerical method used to solve polynomial systems by deforming a known solution into the desired one. Treat the Rule 30 update rule as a polynomial system over $\mathbb{F}_2$, and a putative periodic centre word as a claim that there exists a *fixed point* under a certain deformation (the $p$-step time translation). The lone seed is the initial condition that "nudges" the system.
- **GPT-5.6-H13.0**: **Spark 1 — borrowed from: discrete gauge theory, Aharonov–Bohm holonomy, and the “curvature lives on plaquettes” view of lattice physics.**  
Treat a claimed eventual period \(p\) not as a repeated word but as permission to glue the spacetime diagram’s future to itself after \(p\) time steps: the half-cone becomes a cylinder with a seam at the centre column. Rule 30’s left-permutivity then acts like a Boolean connection: knowing \(C_t,C_{t+1}\) and the right neighbour \(R_t\) transports information one step left via  
\[
L_t=C_{t+1}\oplus(C_t\lor R_t).
\]
A linear rule has flat transport: taking two different local routes around a diamond gives the same answer. Rule 30 may possess a localized “curvature” whenever the \(\lor\) gate engages.
- **GPT-5.6-H13.1**: The object to construct is a plaquette defect \(\kappa(x,t)\) comparing two reconstructions of a cell around a spacetime diamond: reconstruct-left-then-evolve versus evolve-then-reconstruct-left, with the difference recorded in \(\mathbb F_2\). One hopes that \(\kappa\) simplifies to an indicator built from adjacent \(11\) events, perhaps with a telescoping correction. If so, summing \(\kappa\) over a fundamental temporal cylinder has two incompatible readings. The periodic gluing says its total holonomy must vanish: a transported bit returns to itself after going around the cylinder. The lone-seed boundary may instead inject a fixed nonzero flux, or force an odd number of curvature events to cross every such cylinder. This would be a global Stokes theorem, not a fixed-width propagation argument: the useful cancellation is across an entire spacetime region, so the hoped-for route bypasses the logarithmic reach wall rather than fighting it.
- **GPT-5.6-H13.2**: *Rule-90 gut check:* Rule 90’s reconstruction is affine-linear, so its plaquette curvature should be identically zero; that is exactly the desired distinction, although it makes the key task proving that Rule 30’s curvature has a noncancelling seed-sourced flux rather than merely noisy local values.
- **GPT-5.6-H13.3**: **Spark 2 — borrowed from: musical counterpoint, especially the forbidden “parallel motion” rule in a canon, plus error-correcting synchronizing codes.**  
A periodic centre is an ostinato: after time \(T\), the centre sings the same \(p\)-bar phrase forever. But Rule 30 is not a solo instrument. Left-permutivity says the ostinato forces a companion line immediately to its left, provided one knows the line on its right. Repeating that operation generates a whole counterpoint score outward from the centre. The peculiar question is whether the actual right-hand light cone can continually supply accompaniment that makes every newly forced left voice harmonize with the old score.
- **DeepSeek-V4-H14.0**: **Spark 1 — borrowed from: the *no-cloning theorem* in quantum information, and the *monogamy of entanglement*.**  
In quantum mechanics, you cannot copy an arbitrary unknown state; an entangled qubit shared strongly with Alice is necessarily less entangled with Bob. Treat the centre column as a quantum channel that, at each time step, "broadcasts" its bit to its left and right neighbours via the Rule 30 update. The nonlinear \(\lor\) gate is a measurement-like operation that destroys the broadcast fidelity: the centre bit \(C_t\) participates in determining both \(C_{t+1}\) (via the left-permutive preimage) and the right-neighbour's future, but the \(\lor\) with \(R_t\) means the information about \(C_t\) that reaches the left is *not independent* of what reaches the right. A periodic centre would be a cloned signal: the same \(p\)-bit word is regenerated perfectly every cycle, implying that the channel from \(C_t\) to \(C_{t+p}\) has fidelity 1. But monogamy says that if the centre's past is perfectly correlated with its future (the period), it cannot also be the source of the rich, expanding right-half-plane structure that the lone seed actually generates. The correlation has been spent on the rightward expansion, leaving none to lock the centre into a loop.
- **DeepSeek-V4-H14.1**: The Rule-30 object is a *fidelity witness* \(F(t) = I(C_t : C_{t+p}) - I(C_t : \text{right-cone}_t)\), where \(I\) is mutual information measured over the actual lone-seed orbit. Periodicity would force \(I(C_t : C_{t+p}) = 1\) (perfect correlation) while the expanding cone forces \(I(C_t : \text{right-cone}_t)\) to be bounded away from zero (the seed's influence spreads rightward). The no-cloning analogue is that the sum of these two mutual informations cannot exceed \(H(C_t) = 1\) bit, a monogamy bound. The seed saturates the rightward term, leaving nothing for the periodic term. This is not a propagation-depth argument; it is an information-budget argument that asks whether the centre can afford to be periodic given its other commitments.
- **DeepSeek-V4-H14.2**: *Rule-90 gut check:* Rule 90's linearity makes the broadcast a unitary (reversible) cloning of information into superposition; mutual information can be split arbitrarily among many parties because XOR is not monogamous. The centre's eventual zero is the state where all mutual informations decay to zero together, consistent with no conflict.
- **DeepSeek-V4-H14.3**: **Spark 2 — borrowed from: *immunology*, specifically the concept of *original antigenic sin* and the *immune repertoire's memory constraints*.**  
When the immune system encounters a pathogen, it mounts a response and stores a memory of that pathogen. If a related but slightly different strain appears later, the system often reactivates the old memory rather than generating a novel response, even when the old response is suboptimal. The repertoire is finite; each new memory displaces or weakens an old one. Treat the centre column as the immune system's "antibody output" at each time step, and the Rule 30 update as the process of generating that output from the current state of the repertoire (the surrounding spacetime configuration). A periodic centre is a claim that the system has settled into a *fixed memory*: it keeps producing the same \(p\)-step antibody sequence because that's the only memory left. The lone seed is the *original antigen* — the first pathogen encountered at \(t=0\). The hypothesis is that Rule 30's dynamics exhibit original antigenic sin: the seed imprints a memory so dominant that it prevents the system from ever locking into a short periodic cycle. The system keeps "recognizing" the seed's influence in every subsequent configuration, generating a centre that is a continuous, non-repeating variation on that original theme, rather than a fixed loop.
- **Grok-4.6-H15.0**: the Gilbreath principle and the imperfect faro shuffle in card magic.}
A perfect faro is a group element: the deck returns, and the original top card has a known itinerary. Gilbreath is the con’s cousin of that fact — a sloppy riffle of a stacked deck still cannot escape a local colour invariant, which is why the mark never finds the card. Rule 30 is a faro that *sticks*: whenever two face-up cards meet, they adhere and the move drops out of the symmetric group into a monoid.

The Rule-30 object is that monoid acting on a tracked card. Read row \(t\) as a deck, the seed as the original top card, XOR-interleave as the faro generator \(f\), and each \(\lor\)-engagement as a stick generator \(s\). Centre periodicity is the claim that this one card has a cyclic itinerary under words in \(\{f,s\}\). The analogy bites if \(s\) is absorbing on the singleton conjugacy class — clumps form, and returning the tracked card to a lone-card cycle would require an \(s^{-1}\) the monoid does not possess. Gilbreath then sits on the side as a *negative* control: the local colour invariants that survive a riffle are exactly the things a periodic centre would have to keep offering the mark, and \(s\) spends them.

\emph{Rule-90 gut check:} only \(f\), invertible; the top card’s itinerary is the binomial one that is already gone from column 0 after one shuffle.
- **Grok-4.6-H15.1**: the Pythagorean comma and the spiral of fifths (not counterpoint).}
Stacking pure fifths never closes the octave; the leftover is incommensurability, not friction. A canon-spark already asked whether accompaniment can harmonise. This is the other musical accident: two generating intervals whose log-ratio is irrational, so the spiral refuses to be a circle. Treat \(\oplus\) as the octave (torsion, order 2, closes) and a \(\lor\)-engagement as the fifth (the generator that does not commensurate with the octave). A periodic centre is the claim that the spiral closed.

The Rule-30 object is a *comma winding* valued in a circle or a rank-2 lattice, not in \(\mathbb F_2\). To each cell attach an additive mark that \(\oplus\)-updates add and \(\lor\)-updates increment by a second, incommensurable unit (the Boolean shadow of \(\log_2(3/2)\)). Left-permutivity supplies a preferred reconstruction path from \(C_t\) back to the seed, so the winding \(v(t)\) is a function of the actual orbit, not of an ensemble. Periodicity demands \(v(t+p)=v(t)\) along with the bits; the hoped-for lemma is that \(v\) lives in \(\mathbb Z+\mathbb Z\alpha\) with \(\alpha\notin\mathbb Q\) and the seed plus the one-handed \(\lor\) make the second coordinate drift, so closure on the bit also forces closure of an irrational rotation. This is not a heat-bath ledger: nothing is dissipated; two clocks refuse to agree.

\emph{Rule-90 gut check:} there is no fifth, only octaves; the winding is torsion and does close — onto the empty node.

\subsection*{Screen-out}
The no-cloning / monogamy spark is obstruction E wearing a Hilbert-space coat. Mutual information \(I(C_t:C_{t+p})\) and \(I(C_t:\text{right-cone})\) are ensemble quantities; on a single orbit they are not defined, and any sliding-window surrogate reintroduces a measure the lone seed does not possess. The inequality \(I+I\le 1\) therefore cannot pin this diagram. (Original antigenic sin is the same persistence-of-the-seed claim already made by the morphogen front and the origin-trace; it names no new object.)

\subsection*{Open questions}
- Does any 3-cell pattern in Rule 30 *split* a stick, i.e. is \(s\) genuinely absorbing or is there a reset that returns the tracked card to the singleton class?
- Can \(v(t)\) be coupled tightly enough to the bits that it is a shadow of the CA rather than a painted-on decoration? If the second coordinate is just a 11-counter in the past cone, periodicity of bits need not constrain it — that would kill the comma.
- Does reconstructing \(v(t)\) along the left-permutive path still die at the \(O(\log t)\) wall, or does incommensurability let one argue globally (a dense subgroup cannot hit 0 on a cylinder) without walking the path?
- For P3: is reading the next faro/stick generator work, or can a hypothesized period \(p\) let one jump a block of shuffles? If \(s\) destroys invertibility, jumps should be illegal — that would be a work obstruction rather than a depth one.
- **Llama-4-Maverick-H16.0**: the concept of *irreversibility* and *time's arrow* in thermodynamics, particularly the *asymmetric* behavior of macroscopic systems.

Treat the Rule 30 diagram as a thermodynamic system where the centre column is a thermometer measuring the "temperature" of the system. The lone seed is a highly ordered initial state, and the Rule 30 dynamics are akin to a thermodynamic process that evolves this state. A periodic centre column would imply a reversible process, where the system returns to its initial "temperature" after some time $p$. However, the nonlinear $\lor$ gate in Rule 30 introduces irreversibility, as it can create a "mixed" state that cannot be undone. The Rule-30 object is a measure of this irreversibility, potentially quantified by tracking the number of $\lor$-engagements or the growth of the right-half-plane structure. If the centre is periodic, it would suggest that the system has found a way to "beat" the second law of thermodynamics by returning to a previous state.

*Rule-90 gut check:* Rule 90 is linear and reversible; its centre column is eventually zero, consistent with a system that has reached a maximum entropy state and remains there.
- **Llama-4-Maverick-H16.1**: the *garden of Eden* configurations in the context of cellular automata and their relation to *surjectivity* and *preimage counting*.

A garden of Eden is a configuration that has no preimage under the CA rule. For Rule 30, which is known to be surjective, every configuration has a preimage. However, the lone seed is a very specific initial condition. Treat the centre column as a "message" that is being "transmitted" through the CA dynamics. A periodic centre would imply that this message is eventually repeated. The Rule-30 object is the preimage count for the centre column under the alleged period $p$: given a centre bit $C_t$, how many preimages are there that lead to $C_{t+p}$ being the same as $C_t$? The surjectivity of Rule 30 ensures that there are preimages, but the question is whether these preimages can be consistently aligned with the lone seed's influence.

*Rule-90 gut check:* Rule 90's linearity makes preimage counting straightforward; the centre column's eventual periodicity is related to the simplicity of its preimages.

### Open questions
- Can a measure of irreversibility (e.g., $\lor$-engagement count) be defined that distinguishes Rule 30 from Rule 90 and correlates with the non-periodicity of the centre column?
- Is there a way to quantify the "entropy" of the centre column or the right-half-plane structure that grows in a manner inconsistent with periodicity?
- Does the preimage count for the centre column under a hypothesized period $p$ provide a distinguishing feature between Rule 30 and Rule 90?
- Can the surjectivity of Rule 30 be used to argue about the existence or non-existence of a periodic centre column by examining the properties of preimages?

### Screen-out
The faro/shuffle spark, while intriguing, rephrases the existing $O(\log t)$ wall and free boundary concerns in a new context. The key question remains whether the "stick" generator $s$ is genuinely absorbing or if there's a reset mechanism, which is another way of asking about the constraint growth in fixed-depth strips.
- **GPT-5.6-H17.0**: **Spark 1 — borrowed from: Gentzen cut-elimination, Kirby–Paris hydra games, and the strange fact that a proof can get larger every time it becomes simpler.**
- **GPT-5.6-H17.1**: Do not regard \(c_t\) as a bit
- **DeepSeek-V4-H18.0**: **Spark 1 — borrowed from: *cryptographic backdoors* and the *kleptographic kleptogram*, specifically the idea that a deterministic algorithm can embed an undetectable, exfiltrating subliminal channel whose output looks random but carries a signature of its creator.**
- **DeepSeek-V4-H18.1**: A kleptogram is a cryptographic device that hides a secret message inside an apparently innocent output stream, such that only the designer, knowing a trapdoor, can read it. The output passes all statistical tests for randomness, yet it is not random — it is a deterministic encoding of a covert channel. Treat the Rule 30 centre column not as a candidate for randomness, but as a *kleptogram from the seed to the right half-plane*. The seed is the secret planted at \(t=0\). The rightward-expanding structure is the public, "random-looking" output. The centre column is the subliminal channel — the one place where the seed's influence is continuously legible if you know the rule. A periodic centre would mean the kleptogram has a fixed-point loop: the secret is no longer being exfiltrated, it is just cycling. The hypothesis is that Rule 30's nonlinearity acts as a *trapdoor one-way function* on the seed's information: the seed's "message" is injected once, and the centre column is the *only* place it can be continuously recovered, but recovering it requires inverting the \(\lor\) gate, which is lossy. The centre column is thus a *witness* to the seed's persistent influence, not a random output. Periodicity would be the claim that the witness has become amnesiac — it has forgotten the seed and is now just a loop. The Rule-30 object is a *kleptographic invariant*: a predicate on finite centre-column windows that is preserved by the Rule 30 dynamics and is true for the lone-seed orbit, but false for any eventually periodic sequence that does not encode the seed's "signature." This is not a statistical test; it is a cryptographic invariant that only the "designer" (the rule itself) can verify, because it depends on the \(\lor\)-loss structure.
- **DeepSeek-V4-H18.2**: *Rule-90 gut check:* Rule 90's linearity means the seed's message is XOR-ed into superposition and eventually cancels; the kleptogram is empty — the centre column is zero, which is the trivial fixed point of the "forget the seed" operation. There is no persistent witness because the trapdoor is an identity.
- **DeepSeek-V4-H18.3**: **Spark 2 — borrowed from: *topological censorship* in general relativity, specifically the theorem that in a globally hyperbolic, asymptotically flat spacetime obeying the null energy condition, any causal curve that enters a black hole and then emerges would imply a naked singularity, which is forbidden.**
- **Grok-4.6-H19.0**: dendrochronology (crossdating, frost rings, and the master chronology anchored at a known felling year).}

A timber is dated by matching its ring-width and scar sequence against a master chronology that has an absolute year zero, not by staring at the sample until it looks repetitive. A frost ring is an irreversible marker year: a late freeze that leaves a distinct anatomical scar. Treat the lone seed as the known felling year and each \(\lor\)-engagement as a frost ring deposited in the past light cone. A claimed centre period \(p\) is then a *fake timber* — a board whose rings repeat — offered for crossdate against a master that cannot itself be periodic, because marker years are laid down by a growing season that lengthens (the rightward cone) from a single winter.

The Rule-30 object is a *Gleichläufigkeit residual* \(G_p(T)\): for a candidate \(p\), align the centre word on \([0,T)\) with its own \(p\)-shift, and score sign-agreements only at times that are cone-certified frost years (spacetime cells where the \(\lor\) actually fired in the past cone of the centre). Periodicity demands \(G_p(T)\to 1\). The hoped-for bite is that frost years form a Beatty-like, increasingly sparse, aperiodic subset whose characteristic function is not itself eventually periodic, so the restricted agreement cannot tend to 1 unless the cone's marker sequence is periodic, which the seed-as-year-zero forbids. This is a matching of two series, one of them off-column; overwriting column 0 wrecks the residual, so it is not a density-zero continuous functional, and the score is a global inner product against an irregular set of dates rather than a fixed-depth strip.

\emph{Rule-90 gut check:} there are no frost rings, only even-odd earlywood; the master chronology of column 0 ends in empty rings and “crossdates” as a finite prefix plus zeros — the fake timber is genuine because there was never a scar to mismatch.
- **Grok-4.6-H19.1**: the Ship of Theseus, and Hobbes’s sequel in which the discarded planks are stacked and reassembled.}

Bit-equality is the tourist’s question (“is it the same ship?”). Hobbes’s question is whether the *assembly* is the same. Rule 90 answers with parity: any pile of planks with the same XOR is the same vessel, so a completely new hull may still read as the old bit, and the centre is allowed to become the empty ship. Rule 30 answers with carpentry. The next centre bit is not a parity of parents; when the \(\lor\) fires, two particular planks have been pegged and cannot be swapped for any other pair with the same count.

The Rule-30 object is a *typed plank-set* \(P(t)\), the multiset of neighborhood-types (the eight possible triples, or a coarser type that only distinguishes “\(\lor\) fired / did not”) among causal parents of \(c_t\) in the past cone. Eventual period \(p\) on bits is cheap; the analogy bites only if one can prove a rigidity lemma: \(c_{t+p}=c_t\) for all large \(t\) forces \(P(t+p)=P(t)\) as typed assemblies, not merely as parities. The seed is a single unique plank at \((0,0)\); its descendants migrate right, so new type-bearing planks keep entering every sufficiently late past cone from a direction the centre does not control. A periodic assembly would require that migration to be a conveyor that returns the same types, i.e. a periodic right-half-plane, which the lone seed does not supply. Identity of the vessel is then forbidden, and with it identity of the bit.

\emph{Rule-90 gut check:} types collapse to parity, Hobbes’s reassembled pile *is* the ship, and the empty centre is the empty hull made of cancelled planks — Theseus’s problem is soluble exactly when the rule is additive.

\subsection*{Screen-out}
The kleptogram spark asks for a predicate on *finite* centre windows, preserved by the dynamics, true of the seed orbit and false of every eventually periodic word. That is obstruction H in a trench coat: finite data cannot certify an infinite statement, and a window predicate is exactly a kernel/automaticity lower bound. “Only the designer can verify it” is not a route around that; it is a confession that the object is not a proof.

\subsection*{Open questions}
- Can frost years be certified from a centre window plus \(o(T)\) cone queries, or does locating \(\lor\)-firings reimpose the \(O(\log t)\) reach wall?
- Is \(G_p(T)\) forced away from 1 for every \(p\), or only for \(p\) smaller than the mean frost gap (which would be another bounded exclusion)?
- For \(P(t)\): exhibit even one rigidity step — a 3-cell type whose presence in \(P(t)\) is implied by a centre bit — or the plank-set is a painted decoration.
- Does type-rigidity fail for some nonlinear rule that *does* have a bootstrap centre, or is “types don’t reduce to parity, plus a one-sided source of new planks” already the obstruction?
- **Llama-4-Maverick-H20.0**: the concept of *self-organized criticality* (SOC) in complex systems, particularly sandpile models and their avalanches.

Treat the Rule 30 diagram as a sandpile where the centre column is the "output" of the system, and each $\lor$-engagement is an "avalanche" event that redistributes the state. The lone seed is the initial perturbation that sets the system into motion. A periodic centre would imply that the system has reached a stable, non-critical state. However, the SOC hypothesis is that the system remains critical, with avalanches (the $\lor$-engagements) occurring on all scales. The Rule-30 object is the distribution of avalanche sizes and durations in the past cone of the centre column. If the centre is periodic, it would suggest that the system has somehow escaped criticality, which is inconsistent with the observed complex behavior of Rule 30. The key task is to show that the avalanche distribution remains scale-invariant and aperiodic, mirroring the non-periodicity of the centre.

*Rule-90 gut check:* Rule 90 is linear and does not exhibit SOC behavior; its centre column is eventually zero, consistent with a non-critical, stable state.
- **Llama-4-Maverick-H20.1**: the *cutting process* in random matrix theory and the *Sinaĭ's theorem* on the asymptotic behavior of the cutting process.

The cutting process is a stochastic process that arises in the study of random matrices and describes how the eigenvalues of a matrix evolve under certain operations. Sinaĭ's theorem shows that this process has a limiting behavior that is related to the sine kernel. Treat the Rule 30 centre column as a "cutting process" where each bit is the result of a "cut" determined by the surrounding spacetime configuration. The $\lor$-gate engagements are the "cuts" that determine the next centre bit. A periodic centre would be analogous to a cutting process that eventually becomes deterministic and repetitive. The Rule-30 object is the empirical distribution of "cut spacings" (the distances between successive $\lor$-engagements) in the past cone. The hypothesis is that this distribution converges to a non-trivial limit that is inconsistent with periodicity, much like the sine kernel in random matrix theory.

*Rule-90 gut check:* Rule 90's linearity implies that the "cutting process" is deterministic and simple, leading to an eventually periodic centre column.

### Open questions
- Can the distribution of avalanche sizes and durations in Rule 30 be shown to remain scale-invariant and aperiodic, consistent with SOC behavior?
- Does the empirical distribution of "cut spacings" in Rule 30 converge to a non-trivial limit that is inconsistent with periodicity?
- Can the insights from random matrix theory be used to derive a property of the Rule 30 centre column that distinguishes it from Rule 90?
- Is there a way to quantify the "criticality" of the Rule 30 system and relate it to the non-periodicity of the centre column?

### Screen-out
The kleptogram spark is screened out because it essentially rephrases obstruction H (finite data cannot establish an infinite statement) in a different context. The idea of a "kleptographic invariant" that is preserved by the Rule 30 dynamics but is false for eventually periodic sequences is not a new route around the existing obstructions.

## Directions screened out during the panel (by a model, not independently verified)

- [Grok-4.6] The Aumann / agreeing-to-disagree spark. Common knowledge needs a public event. Any encoding of “the centre is periodic” that both a centre-agent and a right-half-plane-agent can deduce is either a spatially extended marker (column-blind under overwrite of column 0) or a finite-strip certificate (free boundary migrates). Iterated knowledge operators quantify over information partitions, which is T3 in epistemic clothing; they do not pin a single orbit.

\textbf{Open questions}
- Does \(H(w,k)\) stabilize, drift, or oscillate as \(k\) grows, for small \(p\)? Stabilization with \(k\) would collapse back into a ladder; genuine \(k\)-drift is the comma.
- Are there “tempered” \(w\) other than the all-zero word for which \(H\) vanishes?
- Is \(\rho\) actually one-way, or does some 3-cell pattern reset it (a grammaticalization that saves the parse)?
- Does residue-counting bound work or only stack height (the VDF/depth trap)?

## Open questions named by the panel

(none)

## Next step (execution phase, separate from this run)

For each candidate direction: (1) check its stated Rule 90 screen in
actual code, not by trusting the model's sketch; (2) check it against
`PATH.md` section 7.3/8.7 for a named obstruction the model may have
missed or misjudged; (3) only then decide whether it is worth a route
entry in `PATH.md` and who owns running it. A direction a model marked
'screened out' is not thereby dead — the screen itself needs the same
independent check as a survivor before anyone relies on it.
Full raw responses, uncompressed, are in `compressed_transcript.json` for
every call — read those before trusting this summary of them.
