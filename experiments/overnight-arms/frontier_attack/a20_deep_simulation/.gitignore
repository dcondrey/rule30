# Generated column data. Regenerable from the verified kernel; see
# CHECKSUMS.sha256 for the digests that pin the runs referenced in
# KERNEL-V6-DEFECT.md and docs/rule30/PATH.md 8.5.
*.bin
!*.bin.sha256

# Live local run (deep_gen.py, PID 51480 as of 2026-08-31).
# depth24h.log is deliberately NOT listed here: it is already tracked as of
# 89182a5, and .gitignore does not apply to tracked files. It will keep showing
# as modified until the 24 h run ends, at which point commit the final log.
depth24h_column.bin
depth24h_checkpoint.json

__pycache__/
