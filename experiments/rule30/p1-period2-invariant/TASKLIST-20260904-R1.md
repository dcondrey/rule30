# Task list: R1-weighted status, 2026-09-04 (late)

Status values: **DONE**, **BLOCKED-LOAD**, **BLOCKED-DATA**, **OPEN**,
**VOID**.

Ordering note: R1 (section A) is weighted first because it is the only open
route in the R1..R7 register — the `p=2` ladder (section B) does not close
Problem 1 even if completed. Section B's compute items are marked
BLOCKED-LOAD: several long-running `n=30` SAT jobs were already in progress
and launching more risked resource exhaustion.

---

## A. R1 — the only open route (highest weight)

| # | task | status |
|---|---|---|
| A1 | Inventory `uc/r1-r1zero/`: establish what was run, what it showed, what is unfinished, **before** writing new code | **DONE** — `RESULTS-R1-ZERO-SET-INVENTORY.md` |
| A2 | Resolve the three tiny `lhp_lock_search*` logs: search-found-nothing vs process-died | **DONE** — all completed; `runs=`/`elapsed` present. 0 lock candidates, violation rate 0.4993-0.5001 vs null 0.5 |
| A3 | Re-verify the pin and pass identities against real Rule 30 | **DONE** — 207 + 193 tests, 0 failures, `T=400` |
| A4 | Establish whether the LHP is closed under `c` alone | **DONE** — **it is**: LHP reconstructed from `c` + zero init matches the true lone-seed LHP over `x in [-400,-1]`, 0 mismatches. The pin never mentions `r` |
| A5 | Check whether `lhp_lock_search`'s zero-lock result is an exclusion | **DONE — it is NOT.** It fixes LHP init to zero, and the PROVED finite-prefix lemma in `docs/rule30/RESULTS-eventual-period.md` blocks any bounded-centre-symbol contradiction. Its tails (9, 14, 65) are pin-only, window-relative measurements and are not comparable to `H(p,w)`; they add no exclusion either way. Do not cite it as evidence against periodicity |
| A6 | State the zero-set obligation as a falsifiable proposition and build the smallest probe | **VOID as stated.** Lone-seed form is trivial: direct computation of `c` excludes all `p <= 64`, onset `<= 10000`, in **0.1 s**. Any lone-seed-specific probe is strictly weaker than `for t in range(T)`. Replaced by A7 |
| A7 | **`H(p,w)` growth.** Was: extend the periodic-prefix horizon table to `w = 9,10,11` at `p=2`, on the theory that a plateau would be the theorem | **DONE — and it was never an open question.** A plateau cannot occur: the finite-prefix lemma already proved in the repo gives `H(2,w) >= w` outright, so this was validation, not measurement. Confirmed constructively to `w = 300` in **0.04 s** with `H - w` in `{1,2,3,5}`, reproducing `H(2,1)=6` exactly and consistent with every published cell. **Extending the SAT/SMT grid cannot close R1 or the `p=2` exclusion.** The kill condition drafted earlier (`H(2,w) > w+8`) was the wrong test and would not have fired on the existing data (`H(2,8)=14` vs 16). Rule 90 control: same behaviour, so this is a left-permutivity fact and a negative about the *method*, not a Rule 30 fact |
| A8 | General-finite-row form of the obligation: does diagram-global consistency restore the forcing that the driven RHP lacks? | **OPEN** — the real R1 question |
| A9 | Apply the Rule 90 filter explicitly, per argument, and record the check | **DONE** — pin arguments PASS (no OR ⇒ no pin; and with `c ≡ 0` the pin has zero tests, so it is vacuous exactly where the statement is false). Driven-RHP forcing arguments are moot, already dead on Rule 30's own evidence |
| A10 | Make the rule a parameter in every future probe and run `rule90_step` as a negative control before believing a Rule 30 positive | **OPEN** — `r1zero_lib.rule90_step` already exists; `driven_halfplane.py` already does it. New code must too |
| A11 | Does `phi/4` say anything about the density of `{c_t = 0}`? | **DONE — no, type mismatch.** `phi/4` governs per-row survival in the rotated wedge; Problem 2 is the true centre column's density, measured **0.500362** at `T=200000`. `drive01_long`'s `|Z| = T/2` is `T/2` by construction and is not evidence |
| A12 | Audit `comoving_columns_T32768.log` (13 KB) — the last unaudited `r1-r1zero` artefact | **OPEN** |
| A13 | Do **not** spend compute on R7 mode (i) | **DONE** — no compute spent; closed by a proved limitation theorem, blocker is a state-count wall |

## B. Compute — all blocked on load 43.70

| # | task | status |
|---|---|---|
| B1 | Extend census to `n=25,26` | **BLOCKED-LOAD**. Also low value: six more zeros on a rung that does not close Problem 1 |
| B2 | `D_k`/margin census on `r=1`, `r=2` at high `n` | **BLOCKED-LOAD** |
| B3 | Launch `n=31` SAT | **NOT DONE.** Launched 2026-09-05 08:34, stopped 08:36. Ran 2m17s, all six logs still 0 bytes, empty logs deleted; no `n=31` cell has ever been solved. The full `n=30` row completed first: six cells, all UNSAT, 34.2 CPU-hours. No runtime estimate is supportable |
| B4 | UNSAT core size vs `n` (`L10-SAT-CORE-SCALING`) | **BLOCKED-LOAD.** (An earlier annotation here said A7 devalued it. **That was a conflation and is withdrawn:** `uc/r1-skeptic/rw_sat_gap_fill_one.py` grids the rotated-wedge census over word length `n`, whereas `H(p,w)` grids the eventual-period SMT probe over support radius `w`. `H(2,w) >= w` says nothing about the `n`-grid.) It remains low-value — more `p=2` zeros do not close Problem 1 — not for A7's |

## C. Score the frozen predictions

| # | task | status |
|---|---|---|
| C1 | Score P1 at `n=22,23,24` | **VOID.** P1 is already falsified exactly (commit `a0f539b`), and the `n=12` tail split (`k_dep=4` at `c=3`, `5` at `c=2`) proves no formula in `n` alone can be correct. No `n=22,23,24` datum can change that |
| C2 | Fit `k_dep(n)` across `n=10..24` | **OPEN**, but demoted: fitting a curve to a quantity that is provably not a function of `n` is only worth doing to bound the residual scale |
| C3 | Demote the light-cone story in `RESULTS-DISTINCT-CONTINUATION-COUNT.md` | **DONE** — correction section added with the full `n=10..21` table, the three failure modes, and the no-tolerance rule |
| C4 | Score P4 (post-departure geometric mean vs `1/phi`); `c=3` was creeping up (0.427, 0.395, 0.530, 0.569) — is it crossing? | **BLOCKED-DATA** |
| C5 | Score P3 (extinction margin) at each new `n`; report the **running minimum**, not the mean | **BLOCKED-DATA** |

## D. Light-cone explanation, tested directly

| # | task | status |
|---|---|---|
| D1 | Measure how many source coordinates determine a length-`k` continuation prefix; compare against `k_seed(L) = ceil((L+1)/2)` | **OPEN** — cheap, and now the *only* thing that can rehabilitate the light-cone story after C3 |
| D2 | Does the departure point coincide with dependency-window saturation of `n`? | **OPEN** — depends on D1 |
| D3 | Exact free-position boundary as a function of `n` and `k` | **OPEN** |

## E. Extinction margin (a parallel investigation's lead — verify, do not assume)

| # | task | status |
|---|---|---|
| E1 | Independently re-derive the margins for `n=10..18` from `literal_extension` | **OPEN** |
| E2 | Audit every numeric claim in `RESULTS-EXTINCTION-MARGIN.md` against the code | **OPEN** — read-only; that file belongs to a parallel investigation, write findings elsewhere |
| E3 | Extract the max-survival words at each `n`; are they a structured family? | **OPEN** |
| E4 | If structured, can that family be excluded by a separate exact argument? | **OPEN** — this is the one that would be a real theorem fragment |
| E5 | Why is the margin tail-dependent (`c=2` rising, `c=3` flat)? | **OPEN** |

## F. The distinct-continuation object

| # | task | status |
|---|---|---|
| F1 | Multiplicity distribution vs `n`; is mean multiplicity `~ 2^n/D`? | **OPEN** |
| F2 | Law for `D_k`'s peak location and height | **OPEN** — treat with C3's caution: 3+ values before asserting |
| F3 | Is post-departure decline better modelled by the null transfer matrix than the Fibonacci bound? | **OPEN** |

## G. Null model

| # | task | status |
|---|---|---|
| G1 | Forced-symbol uniformity at `n=18,20` — does deviation keep shrinking? | **OPEN** |
| G2 | Does the null model predict the `D_k` peak position? It predicts survival rate, not distinctness | **OPEN** |
| G3 | Exact null-model prediction for `D_k`, overlaid on measured | **OPEN** |

## H. Audit and consolidation

| # | task | status |
|---|---|---|
| H1 | Re-verify `13/32` uniformity at `n=10,11,12` with exact rationals | **OPEN** |
| H2 | Grep for other "32/32 complete branching" citations and annotate | **OPEN** |
| H3 | Cross-check `rw_population_h.forward_filter` against a third filter | **OPEN** — file belongs to a parallel investigation; read only |
| H4 | Audit `PROOF-STATE-CAPSULE.md` §5 against current findings | **OPEN** |
| H5 | Update `BACKLOG.md` L11 with the latest results | **OPEN** — coordinate; a parallel investigation edits this row |

## I. Write-up

| # | task | status |
|---|---|---|
| I1 | Update `RESULTS-DISTINCT-CONTINUATION-COUNT.md` with `n=17..24` and re-score its four checks | **PARTIAL** — the `n <= 21` P1 rescore is in (C3); the `n=22,23,24` data does not exist yet (BLOCKED-DATA) |
| I2 | Update `RESULTS-CROSS-METHOD-INVARIANT-AUDIT.md` if any constant moved | **OPEN** |
